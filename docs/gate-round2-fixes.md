# 计划门第 2 轮：终判与剩余 6 处（给 Session 2）

> **依据**：独立只读复核（逐条验 23 项，行号= `docs/plan-0.8.0-execution.md`）。
> **结论**：**21 项 PASS**（含 `0.1 / 0.2 / 0.3` 三条硬检查——F16 与 F18 均已转正），**只剩 F17 的两个子情形 + 三处改漏的旧文本**。
> ⇒ **仍退回修订，但只改下面 6 处，其余一律不要动**（动别的就会再引入新矛盾，这正是前两轮的主要失分类型）。

---

## 1. 必修 6 处

| # | 要改的 | 依据 |
|---|---|---|
| **H-1** | **F17 补优先级**：`ref == expectedOld` **且** `mergeAttempts` 已有同 attemptId 时，②（按 intent 重做）与 ④（幂等同值）**冲突且无优先级** ⇒ 写明先判哪一段，并把判定顺序写进四段表 | 复核第 2 项 PARTIAL |
| **H-2** | **F17 补"intent 落盘中途崩溃/截断"的规则**：全文 0 处 `rename`/`fsync`/临时文件 ⇒ 明确"用临时文件 + 原子 rename（或等价）写 intent；**解析失败视为无 intent**，回落到既定分支" | 同上（该子项 UNKNOWN） |
| **H-3** | **§0.2-B-2（约 50 行）** 括注仍写"（§7 给出两跳写法与回退写法）" ⇒ 改为单跳 | 新矛盾 ① |
| **H-4** | **附录「偏离一览」（约 860 行）** 仍写"§7 部署改为 profile patch 层两跳写法" ⇒ 改为单跳 | 新矛盾 ② |
| **H-5** | **§9.1（约 722 行）"在'审查节点合计'里报 1 次" × §11（约 850 行）"审查型 0 新增"** ⇒ 写清两个口径：「**新增**」= 相对基线的**额外**动作；「**合计**」= 该节点**全部**动作数。并让 `verify-agent-budget.mjs` **分开断言**（如 `review_new == 0`）——现 `expect<=4` **判不出 0 新增** | 新矛盾 ③ |
| **H-6** | §G.5 自查写"16 个脚本"、实测 **17** ⇒ 改数字（属"声明不实"类） | 复核第 5 项 |

## 2. 本门两条裁定（**记录在案，后续不得再翻**）

1. **agent 预算接受为「典型 ≤3 / 上界 4」**：第 4 次触点**仅出现在确有真实请示时**；判据 9 的"≤3"按**典型值**理解。
   理由：第 4 次由**真实请示**触发，不是流程摊派；且它已给出算式与"为什么不能由宿主承担"（被摄入的是**执行节点**、不是 driver，两者不是同一会话）。⇒ §9.1/§9.2/§11 G-BUDGET 的现有写法**保留**。
2. **§3.3-2 字面"断言它不发起审批"判定为做不到，接受可判定弱形态**：
   `dsh-user-approval` 对**任何**请求都**无条件先 append `approval/asked`**（`:134-146`），`never` 只让 `decide()` 不派发 waterfall 直接 `rejected`。
   可判定形态 = **拒绝即时 + `asked/decided` 成对 + 回合有 `turn/end`**。
   **round-8 已告知 UCBIP，其回应"正是我们要的"** ⇒ **视为双方已同意**；文档如实标注"对需求字面的弱化"即可，**不再回问**。

## 3. 第 3 轮要求

1. **只改上面 6 处**，其余一律不动；
2. 交回**增量对照表**（逐条：改前 → 改后 + 行号），其余不必重发；
3. 我复判**只验这 6 处 + F17 四段表是否真的穷尽**；**通过即门判 PASS**，随后连同门意见交 **ChatGPT Pro 二审**（它的输入 = 修订后的计划 + 我的门判定 + 前两轮的修订清单）。

## 4. 附：已确认通过的部分（供对照，不必再看）

F16 单跳 + §7.3-a 直接调宿主 `composeEntries`｜`inputs[]` 为唯一声明字段、`extraInputs[]` 已全局删除（F21）｜F18 与 F20④ 写在同一处（"按哈希显式绑定"的两面）｜F19 摄入改回 prepared 且 §1/§9.1/§9.2/§11 同步｜F20 四条限定齐备｜F22 下界非上界 + 计入证据质量｜F23 预算算式与 G-BUDGET 同步｜弱形态已明文标注｜预设目录 12 与 `healthy=12/12`（`13/13` 残留 0）｜闸点名 17 个脚本 100% 在 §5.1 清单里。

---

## 5. 第 3 轮复验与**终判**

**复验结果（独立只读，920 行全文抽查）**：
- **H-1…H-5 全部 PASS**：H-1 六行有序表 + **①vs③ 重叠回归用例**（`ref==expectedOld` 且已有同 attemptId ⇒ 必须走①、ref 不动）；H-2 原子写（`.tmp.<pid>` → `fsync` → `rename`）+ ⑤行"视为无 intent、判不了即 `recovery_indeterminate` 进阻断清单、不猜" + **对 `rename` 的 grep 断言**；H-3/H-4 旧"两跳"已清（全文剩 5 处均为解释性/否定性）；H-5 两口径写清 + `review.new == 0` **硬断言** + §5.1/§11 逐字同步。
- **H-6 PARTIAL（材料层面已正确）**：26 文件 / 28 命令、`20 具名 + 6` 表述正确、**闸点名 26 个 − §5.1 清单 = 空集**、重复的 N6 行已删；仅两处收尾未做（见下表 R-2）。

### 5.1 终判：**门判 PASS**（附 4 条清理项，**清理项不再回门复判**）

计划已具备执行条件。4 条均为**一行文本/表格级**修正，不涉及任何语义、判据或硬检查：

| # | 清理项 | 依据 |
|---|---|---|
| **R-1** | @129（P2-5 工作项）仍写"**四段**幂等恢复（§4.2 步 6）" ⇒ 改为"**六行有序表（§4.2.1）**" | 新矛盾 ① |
| **R-2** | §5.1@551 仍是一行**斜杠组**、命令列用 `verify-negative-N<k>.mjs` **占位** ⇒ 拆成 6 个具名行 + 写实际文件名 | H-6 残留 |
| **R-3** | §11 枚举表 @884 记 G-P3 为 **4** 脚本/4 命令，同一闸 @900 仍写"**三个脚本**" ⇒ 以枚举表为准统一 | 新矛盾 ② |
| **R-4** | 全文再扫一遍"数字类陈述"与"脚本计数"，确认没有第三处不一致 | 三轮唯一的失分类型 |

**给执行者（Session 3）**：R-1…R-4 开工时顺手改掉、在首次提交里体现即可，**不必回门**。
**给计划维护者的类问题建议**：三轮的失分**全部**是"改一处后旧文本没同步"。有效解法你们自己已经证明了——**对易漂移短语加 grep 断言**（如 `rename` 那条）。建议再加三条：`四段`、`两跳`（仅允许出现在解释性位置）、以及"每闸脚本数 ↔ §11 枚举表"的一致性检查。

### 5.2 门判结论与下一步

- **门判：PASS**（23 项修订 + 6 项清理中 5 项已验、4 项清理随首次提交落地）。
- **下一步**：连同本门判定 + 两轮修订清单交 **ChatGPT Pro 二审**（输入 = 修订后的 `docs/plan-0.8.0-execution.md` + `docs/gate-round2-fixes.md` + 本文件 §1–§5）；二审通过后交 **Session 3** 执行。
- **附带登记的一条真实缺陷（给 D4 守卫当首个用例）**：`a2a_send` 调用失败并报 `cannot get property "sandboxPolicy" without inject`——**A2A 投递路径上有未声明 `inject` 的服务访问**，正是 D4（工具闭包接线）要防的成类回归。
