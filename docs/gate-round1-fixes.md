# 计划门第 1 轮：修订清单（给 Session 2）

> **判定**：**修订后可过**（不重写）。第 1–5、7 条 PASS；第 6、8、9 条 PARTIAL；四条"退回"里 **R-1/R-2/R-3 需收口、R-4 的退回理由不成立**。
> **硬检查 0.1–0.3 与本报告分开判**（见 §C，待第 2 份报告并入后再给终判）。
> 依据：计划门第 1 轮子代理报告（第 1–9 条 + R-1..R-4 + §0.2/§9.2）。以下每条给：**改什么 / 依据 / 判据编号**。

---

## §A 必修（按判据编号）

| # | 要改的 | 依据（子代理报告） | 判据 |
|---|---|---|---|
| **F1** | **`inputs: []` 一律视为"未声明"**（不是"已声明"）；**变更路径必须由插件用 git diff 自算**，不得取自角色。两条写进 §1.3 / §1.4 与 §4.1 步 6′ | 空数组会被认作已声明 → 交集为空 ⇒ 判 `reused`，**等于把 §1.4 明令禁止的"我只依赖这里"放回来** | R-1 |
| **F2** | **`reuseScope` 必须含「模式」与「外呼宣言」**，不能只到 `envDigest` | 与 P2-4 用例③「历史外呼读数不得证明本次真链」**自相矛盾**；契约 §1 的运行条件含树/环境/模式/外呼 | R-2 |
| **F3** | **R-3 的两档要补"收束出口"**：`unqualified` 不是终点 ⇒ 必须走"交给人裁决"或明确的 blocked 终态 | 否则**输出不可解析的仓库永远 `unqualified` ⇒ 无有效证据且节点无出口**（活锁）；缺契约 §0 约束三的收束 | R-3 |
| **F4** | **R-4 的理由重述**：原要求（处置列表为空即拒）**本身可机械判定**；真问题是字面执行会变成填表/活锁 ⇒ 这是**语义澄清，不是"不可判定"** | 退回理由不成立（按"规则一"标注不当） | R-4 |
| **F5** | **显式标注一处移位**：强制摄入的时机从需求的"**准备阶段**"移到 **freeze 时**（§9.1）⇒ 不申报候选的节点**永不摄入** | 该移位未标注；需 UCBIP 确认 | R-4 |
| **F6** | **P1-1 的措辞冲突**：明确"命令在 **freeze 时由节点提议、由门冻结并记录**；**运行期 runner 只从记录里查**，模型不得临时提供命令"；§9.1 freeze 参数里的"验收清单"改述为"**验收清单提案（含 command），冻结后不可改**" | P1-1 括注"模型永不提供命令字符串"与 §9.1 freeze 参数（§6 清单含 `command`）**措辞冲突** | 4 |
| **F7** | **谁冻结清单写死**：清单由节点**提议**，**门（插件）冻结并作为记录作者** | "谁冻结清单"未定，擦契约 §0 **约束二**（事实的作者永远是插件）的边 | 5 |
| **F8** | **预设目录计数：13 → 12**（3 legacy + 9 v0.4，已 find 枚举）；§5.1 / §7.3-a 的期望改 `healthy=12/12` | 13 与本机实测不符 | 8 |
| **F9** | **E4 的"测试已有该断言"**：`scripts/test-orchestra-role-presets.mjs` 里 grep 不到 `danger-full-access` ⇒ 要么**补该断言**，要么措辞改"**将**新增该断言" | 声明不实（不得把未验证写成现状） | 8 |
| **F10** | **agent 预算重算**：把**审查节点的"写审查记录"**与**合并门的触发动作**计入；附页"新增五个 agent 工具"与实际点名的三个对齐；`orchestra_defect_disposition` **已并入 freeze**，不得同时列为独立工具 | 三处自相矛盾 | 9 |
| **F11** | **§10.6 补两行 `fault_ref`**：`src/candidate.ts` → A1↔P15；`src/baseline.ts` → A7↔P8 | 两个新机制没进故障对照表 | 6 |
| **F12** | **§5.1 清单补两个脚本**：`verify-negative-N6.mjs`（G-P3 点名）、`test-orchestra-role-presets.mjs`（E4 的闸） | 闸里点名的脚本不在交付清单 | 3 |
| **F13** | **B3② 的出处标注**：在 §0.3 或 §10 注明"沿用契约 §1.2 B3 行的收窄（② 只登记、不阻断）"，不要让它看起来像静默收窄 | 追溯性（判据 1）要求显式 | 1 |
| **F14** | **N6 的兜底措辞**：把"被杀前已生成过胶囊"标为**依赖 R-11 兜底**，或在正例里**显式先生成胶囊** | 现在读起来像产品保证 | 2 |
| **F15** | **包名钉死**：`ctx.shell` 的提供方在两处不一致（B-4 说 `@deepseek-ai/dsh-bash-sandbox`；§2.2/R-08 说 `@deepseek-ai/dsh-shell`）⇒ 统一成实测的那个；并把 B-4 的替代项补上后半句「**若不以宿主身份运行 ⇒ P1 全部证据不合法**」 | 包名不一致 + 替代覆盖不全 | C-1 |

## §B 不需要改、但要在计划里回一句的

- **§9.2（为什么不能压到 2 次工具调用）站得住**：把 `decision_open` 机械推导掉等于**宿主替 agent 断言"你没有问题"**，落在契约 §0 约束三/二之外。保留。
- **判据 7（不做清单）PASS**：五条以"不做"正面出现并有 grep 断言；"不因未跟踪文件整批拒"与 P1-3 判 `unqualified` 不冲突。
- **B3② 的收窄**（"canonical owner 未定 ⇒ 拒绝开工" → 只登记）：**与契约 §1.2 B3 行一致**，不是计划的擅自收窄——只需按 F13 标注出处。

## §C 终判（硬检查报告已并入）：**退回修订**（修订面窄，不重写）

> 本节取代文首"判定：修订后可过"一句。**最终修订清单 = 15 条（§A）+ 3 条（§C.1）= 18 条。**

### C.0 硬检查 0.1–0.3

| 硬检查 | 判定 | 依据 | 缺口 |
|---|---|---|---|
| **0.1 冻结快照执行** | **PASS** | §P1-1 `worktree add --detach`；§P1-3 `extraInputs[]`＝路径 + sha256，未声明即 `unbound_input`；§4.3d `unknown ≠ 合格` | 缺"**已声明哈希由谁、何时重算**"的规则（**F18**） |
| **0.2 活跃租约冲突阻断** | **PASS** | §P2-7 越界触活跃租约 ⇒ **阻断冻结/合并、豁免不取消**；§P2-6 **打印全量豁免**（理由/人/时间） | —— |
| **0.3 `update-ref` + 崩溃幂等恢复** | **PARTIAL** | §P2-5 用 `update-ref <expected-old>`；§4.2 明写"预演不是 CAS" | §4.2 步 6 的恢复要与"记录的 `expectedNew`"比对，但 §6 节点记录字段表**没有 `expectedNew` 的落点**（只有 `mergeAttempts[].expected-old`）⇒ **崩溃后无值可读**（**F17**） |

### C.1 三条"关键发现"的核查

| 发现 | 判定 | 说明 |
|---|---|---|
| ① §1.1 部署写法在本部署不成立 | **部分属实（且计划的修法是错的）** | insert 确在 `dsh-web-app/cordis.patch.yml:481`；web / dev 两个 profile patch 都是 `[]`；浅替换陷阱**已实测复现**。**但计划的"两跳"写法会插出一条新的 `agent-presets` 行**，与 bundle 原行并存两条同名条目 ⇒ 见 **F16** |
| ② D2 的机制级成因 | **完全属实** | 钉点仅 `session-blueprint.ts:826`；`orchestra.ts:3084` 裸 resume、`:3611-3621` 重激活 setup 只含 `mountPreset`+`installModelOverride` ⇒ 均不钉；`dsh-base/cordis.patch.yml:238` = `approval: ask`；`dsh-user-approval/lib/index.js:134` **无条件 append `approval/asked`** → `:141 decide` → `:178 never ⇒ rejected` → `:179` 才走 waterfall |
| ③ S2 的修复点已存在 | **属实** | `dsh-api-session-controller/lib/index.js:395-406` ＝ `composeAgent` + `resume({setup})`；自建路径在 `src/a2a-transport.ts:177-235` |

**两条硬依赖**：**均属实**。`dsh-agent-presets/lib/index.js:483` 只把 `trust==='user'` 当可写根（`:591` notWritable），插件确实自写（`src/orchestra-role-presets.ts:931` 用 `wx`）；`ctx.shell` 的**服务定义在 `@deepseek-ai/dsh-shell`**（`lib:86`）、**提供者是 `@deepseek-ai/dsh-bash-sandbox`**（`:105`）⇒ **要声明的是 `dsh-shell`**（进 dev + peer，**禁 `dependencies`**，AGENTS.md:25/28）。

### C.2 新增三项必修

| # | 要改的 | 依据 | 判据 |
|---|---|---|---|
| **F16** | **§7.1 改成「单跳 override」**：profile patch 里写 `- id: agent-presets` + `config: { default: standard, roots: [{ path: ~/.dsh/orchestra/catalog-presets, trust: system }] }`（**必须自带 `default`**）。**删掉"两跳"**。并**在实现前用同样的内存实测确认「profile 的 patch 层在 bundle 的 patch 层之后应用」**——若顺序相反，单跳只会 warn `entry not found` 而静默不生效 | 引擎实测：后一层 patch **可命中**前一层 insert 的行（`cordis-plugin-include/lib/index.js:70-84`），故两跳既无必要也未达目的；浅替换 `:100-103` 已复现 | 契约 §1.1 |
| **F17** | **为崩溃恢复补"写前意图 + 预期新值"**：§6 节点记录加字段（如 `mergeIntent{ expectedOld, expectedNew, at, attemptId }`，**在 `update-ref` 之前落盘**），§4.2 步 6 的恢复改读它；或明确"用 §4.2 步 1 的 `previewedTree` 做比对"并写清判定 | §6 无 `expectedNew` 落点 ⇒ 0.3 的幂等恢复落不了地 | 0.3 |
| **F18** | **补「已声明哈希的重算规则」**：`extraInputs[]` 的 sha256 由**谁、在何时**重算（建议：宿主在**每次**验收执行前重算并比对，不一致即 `unbound_input`，**不得沿用上次读数**） | 0.1 只写了"未声明 ⇒ `unbound_input`"，没写"声明了但内容已变" | 0.1 |

### C.3 UCBIP 三条裁定（round-8，**已到**）→ 派生 **F19–F23**

> ⚠️ §C 开头那句"最终修订清单 = 15 + 3 = 18 条"**已过期**：**现为 23 条**（§A 15 + §C.1 3 + §C.3 5）。

| # | 要改的 | 依据（裁定要点） |
|---|---|---|
| **F19** | 强制摄入时机**改回「准备阶段（prepared）」**（§9.1 现写在 freeze 时）；并补「**相关缺陷集 + 每条处置纳入候选身份的决策快照**」⇒ freeze/merge 侧若相关缺陷集变了由 A1 自动失效（generation+1、旧审查旧证据作废），**两侧都覆盖、不加第二个检查点** | 裁定：它的作用是在动手前影响动手的方式；放到 freeze 就从"事前约束"退化成"事后签字"，而且**不申报候选的节点永远不会被摄入** |
| **F20** | **`inputs[]` 四条限定**：① 属**冻结验收协议**（改它 = 协议变更 = generation+1）；② 审查者审的是"**这个声明是否正确**"，不是"这次复用是否安全"；③ 复用时**标注"基于声明输入集、完备性未证"**，并**计入 §1.9⑤ 证据质量节**；④ **必须覆盖非 git 跟踪的输入**（夹具 / env 文件按 sha256 绑定） | R-1 接受时附带的三条限定 + 一条澄清 |
| **F21** | **统一 `inputs[]` 与 `extraInputs[]`**（§P1-3）：**是一个字段还是两个？**给出唯一口径并全局替换；若保留两个，写清各自归属与关系（如"`inputs[]` 是声明面、`extraInputs[]` 是其运行时解析结果"）。**不许两处各说各话** | 我方复判新暴露的一致性风险（同一件事两处表述，正是本轮主要失分类型） |
| **F22** | `reuseScope` 写清"**下界不是上界**"：五项闭集是**必须比对的下界**，不是"只看这些"的上界；插件已采集的其它身份字段（解释器 / 工具链 / 依赖目录等）若不同 ⇒ **至少告警 + 计入证据质量节，不得静默放行** | R-2 接受时附加的一句 |
| **F23** | **重算 agent 侧预算**：摄入改回 prepared 后，§9.1 原算式"处置搭在 freeze 上 ⇒ 0 次新增"**不成立**；重算（能否并进"申报"那一次仍是 3 次，还是会变 4 次），并同步 §9.2 的论证 | F19 的直接后果 |

### C.4 交付物与顺序（已定）

- **21+2 条全部可落**，原"等 UCBIP 再落 F1/F4/F5"的顺序指令**作废**；建议顺序：**硬检查三条（F16/F17/F18）→ 裁定派生五条（F19–F23）→ 其余（F1–F15 + 三处硬伤）**。
- **唯一外发产物 = 修订对照表**（UCBIP 明确"只发改动点与判据，不必重发全文"）；表末附三行：① 硬检查复验结论（含 F16 的**补丁引擎内存实测结果**）；② agent 预算最终数字（F23）；③ 不同意/做不到的条目（带证据，不得静默偏离）。
- 收口后把 F19–F23 **追加进本文件**（保持清单完整，供复判对照）。

## §D 需求侧裁定状态（round-8，**已闭环**）

| 裁定 | 结果 | 落到哪几条 |
|---|---|---|
| **R-1** `inputs[]` 细粒度复用 | **接受**（其版本：默认整树失效；只对冻结清单里声明 `inputs[]` 的验收开放；`inputs: []` 视为未声明；变更路径由插件 git diff 自算）+ 三条限定 + 一条澄清 | **F1 + F20** |
| **R-2** 五项闭集 | **接受**（候选树 + 命令 + 环境摘要 + 沙箱/权限模式 + 外呼宣言；A6 措辞随之收窄）+ 一句防误读（下界非上界） | **F2 + F22** |
| **R-4** 机械谓词 + 时机 | **谓词接受**（"相关缺陷候选非空 ∧ 未逐条表态 ⇒ 拒；候选为空 ⇒ 过"）；**时机不接受移位，改回 prepared** | **F4 + F5 + F19** |
| 我方新增 | **F21**（一致性收口）、**F23**（预算重算）——纯内部，不需对方确认 | F21 / F23 |

**四条告知（对方明确无需动作）**：① §1.1 定为单跳 override（部署步骤的坑，不影响需求）；② D2 机制级成因属实 ⇒ **D2-a 从原则变成可判定形态**；③ S2 改为"停止重造、用原生 `resumeObserved`"（对方评价为本轮最好的一条）；④ 两条硬依赖属实（`@deepseek-ai/dsh-shell` 进 dev + peer、禁 `dependencies`）。

---

## §E 计划门第 2 版新增五条（F19–F23，来自 round-8 裁定的派生项）

> 来源：计划门 Session 1 第 2 版消息。**21 条全部可落**，"等 UCBIP 再落"的顺序指令作废。

| # | 要改的 | 依据 | 判据 |
|---|---|---|---|
| **F19** | **强制摄入时机改回「准备阶段（prepared）」**（§9.1 原写在 freeze 时）；并补"**相关缺陷集 + 每条处置纳入候选身份的决策快照**"⇒ freeze/merge 侧若相关缺陷集变了，由 A1 自动失效（`generation + 1`、旧审查旧证据作废），**两侧都覆盖、不加第二个检查点** | 裁定：它的作用是在动手前影响动手的方式；放到 freeze 就从"事前约束"退化成"事后签字"，而且**不申报候选的节点永远不会被摄入** | R-4 / F5 |
| **F20** | **`inputs[]` 四条限定**：① 属**冻结验收协议**（改它 = 协议变更 = `generation + 1`）；② 审查者审的是"**这个声明是否正确**"，不是"这次复用是否安全"；③ 复用时**必须标注"基于声明输入集、完备性未证"**，并**计入 §1.9⑤ 证据质量节**；④ **必须覆盖非 git 跟踪的输入**（夹具 / env 文件按 sha256 绑定） | R-1 接受时附带的三条限定 + 一条澄清 | R-1 |
| **F21** | **统一 `inputs[]` 与 `extraInputs[]`**：给出唯一口径并全局替换；若保留两个，写清各自归属与关系。**不许两处各说各话** | 同一件事两处表述——本轮主要失分类型 | 一致性 |
| **F22** | `reuseScope` 写清"**下界不是上界**"：闭集是**必须比对的下界**；插件已采集的**其它**身份字段（解释器 / 工具链 / 依赖目录等）若不同 ⇒ **至少告警 + 计入证据质量节，不得静默放行** | R-2 接受时附加的一句 | R-2 |
| **F23** | **重算 agent 侧预算**：摄入改回 prepared 后，原算式"处置搭在 freeze 上 ⇒ 0 次新增"**不成立**；重算并同步 §9.2 的论证 | F19 的直接后果 | 9 |

## §F 第 2 轮修订的落地情况（F1–F23 全部已落）

> 唯一外发产物 = **修订对照表**（`docs/plan-0.8.0-execution.md` 的改动点，逐条"改前 → 改后 + 所在节号"）。
> 落点：`docs/plan-0.8.0-execution.md`（§0.2 / §0.3 / §1 / §2 / §3 / §4.1–§4.3.2 / §5.1 / §6 / §7.1–§7.4 / §8 / §9.1–§9.3 / §10.6 / §11）。

### F.1 三条硬检查的复验结论

| 硬检查 | 复验 | 依据 |
|---|---|---|
| **0.1 冻结快照执行** | **PASS（转正）** | F18 补齐"已声明哈希的重算规则"：**宿主在每次验收执行前逐项重算、不一致即 `unqualified`/`input_digest_mismatch`、不得沿用上次读数**（§4.3.2）；F21 把 `inputs[]` 收成**唯一**声明字段（`source: "git"｜"non-git"`，后者必带 `reason`），F20④ 保证非 git 输入被盖住；F1 补"`inputs: []` 视为未声明"、"变更路径由插件 `git diff` 自算"。**F18 与 F20④ 已按要求写在一起（§4.3.2 开头明说两者是同一件事的两面）** |
| **0.2 活跃租约冲突阻断** | **PASS（维持）** | §P2-7 越界触活跃租约 ⇒ 阻断冻结/合并、豁免不取消；§P2-6 打印当次全量豁免集（理由/加入人/时间）。本轮未改动 |
| **0.3 `update-ref` + 崩溃幂等恢复** | **PASS（转正）** | F17 补落点：新增 **`orchestra/nodes/<node-id>.intent.json`**（`MERGE_INTENT_SCHEMA_VERSION = 1`，含 `expectedOld` / `expectedNew` / `previewedTree` / `attemptId`），**在 `update-ref` 之前落盘**。§4.2 步 6 的恢复改成**四段判定**（== expectedNew ⇒ 补记账不再 update-ref；== expectedOld ⇒ 按 intent 重做；第三个值 ⇒ 判 `base_advanced` 回预演；已有同 attemptId ⇒ 幂等删 intent） |

**F16 的补丁引擎内存实测结果**（探针 `/tmp/f16-order.mjs` 与 `/tmp/f16-probe.mjs`，被测引擎是发货的 `dsh-app-boot`：`composeEntries` 直接 `require`，`applyEntryPatches` 从模块正文提取后执行）：

| 探针 | 输入 | 实测输出 |
|---|---|---|
| **A** | `composeEntries([bundlePatch, profilePatch, [], []])`；bundle `insert` 一行 `agent-presets`（`{default:"standard"}`）；profile **单跳** `- id: agent-presets` 带 `default` + `roots` | `[{id:"agent-presets", config:{default:"standard", roots:[{path:"~/.dsh/orchestra/catalog-presets", trust:"system"}]}}]` —— **恰好一行**、config 为覆盖后的值、**0 warning** |
| **B** | 同上但层序对调 `composeEntries([profilePatch, bundlePatch, [], []])` | `[{id:"agent-presets", config:{default:"standard"}}]` + warn `patch: entry agent-presets not found` ⇒ **覆盖丢失**，证明层序承重；而 `composeProfile` 的 `allPatches` 正是 `bundlePatches → profile.patches → homePatches → overlays`，所以 (A) 是真实路径 |
| **C** | 原计划的**两跳**写法 | **两条** `id: agent-presets` 行（一条 bundle 的、一条新插入的），两条 config 都是新的 ⇒ **既未覆盖也未去重**。**已从 §7.1 删除** |
| **D** | 单跳但**不带 `default`** | `config = {"roots":[…]}` ⇒ `default` 被整个顶掉 ⇒ **浅替换陷阱复现**，§7.1 的"必须自带 `default`"由此确立 |

**结论**：§7.1 改为**单跳 override**（F16 落实），并把四条探针结果写进 §7.4；`verify-role-presets-roster.mjs` 要求**直接调宿主 `composeEntries`**（而不是自己模拟层序），层序一变立刻红。"两跳"的失败模式（两行同名）已登记进 R-02。

### F.2 agent 预算最终数字（F23）

| 节点类型 | 新增工具调用 | 明细 |
|---|---|---|
| **交付型（实现者）** | **典型 3 / 上界 4** | prepared 摄入 **1** + freeze **1** + capsule_submit **1**（+ 确有请示时 `decision_open` **1**） |
| **审查型（reviewer）** | **0 新增** | 既有的 `orchestra_report` 被加必填字段（`reviewedCandidateId` + 对 `inputs[]` 声明的意见），**不新增工具/调用**；但在预算核对里按 1 次计入 |
| **合并门触发** | **0** | 由**插件**在"候选已冻结 + 审查 PASS 已记录 + 证据齐备"时判定，调用者是既有工具；不新增工具 |
| **driver / 档 0** | **0 / 0** | driver 走既有工具；档 0 交付层整体关闭 |

**与 2–3 目标的差距**：上界 **4 超出 1 次**，已按 brief §2 第 9 项说明"为什么不能由宿主承担"（§9.3）：① R-4 的**谓词**在宿主侧可判，但**逐条处置的内容**是判断（`alignment-0.5-position.md` B2"允许一句话理由、不要求论证"），**不是可推导字段**；② 唯一不违反 F19 的替代是"让 driver 在任务卡里携带摄入处置"，但**被摄入的是执行节点、不是 driver**，两者不是同一会话 ⇒ **该替代不成立**。新增 G-BUDGET 闸 + `verify-agent-budget.mjs` 做机械核对（`over_budget 0`）。

### F.3 不同意 / 做不到的条目

**没有"不同意"的条目；有一条"做不到"、一条"部分做不到"，都已改成可判定的弱形态并留在计划里（未静默偏离）**：

| 条目 | 状态 | 依据与替代形态 |
|---|---|---|
| **需求 §3.3-2 的字面措辞"断言它不发起审批"** | **做不到**（不声称） | `capability-boundaries.md` **#1**：只保证「请求不挂起 + 确定性拒绝 + 留痕」，**不保证模型不会尝试**；而 `dsh-user-approval` 对 **任何**请求都**无条件先 append `approval/asked`**（`lib/index.js:134-146`，`never` 只是让 `decide()` 不派发 waterfall 直接返回 `rejected`）。**可判定形态 = D2-a 步骤 2–5**：拒绝是即时的、`asked/decided` 成对、回合有 `turn/end`（§4.8）。**不写做不到的断言** |
| **§1.3 的"无法证明不受影响"要算路径交集** | **部分做不到**（R-1，计划门已接受其收窄版本） | 算交集需要"这条验收读了哪些文件"这一事实，而它只能从角色自述或自建依赖分析器得到——契约 §1.4 与 §3-P1 两次明令不做后者。替代：**默认整树失效**；只对声明了非空 `inputs[]` 的验收开放细粒度复用；变更路径**由插件 `git diff` 自算** |
| **F19 带来的预算上界 4 次** | **超出目标 1 次，但无法在不违反 F19 的前提下压回 3** | 见 F.2；已按要求给出算式与"为什么不能由宿主承担"，并新增 G-BUDGET 闸 |
| **N6 的"被杀时已有胶囊"** | **降级为用例内显式步骤**（F14） | 胶囊生成时机（`turn-stopping`）本身是 R-11 的兜底项，所以正例**显式先生成一次**，不写成产品保证 |

---

## §G 修订对照表（**唯一外发产物**）

> 23 条逐条"改前 → 改后 + 所在节号"。**只发改动点与判据**，不重发全文。
> 全部改动落在 `docs/plan-0.8.0-execution.md`；本文件 §E/§F 为清单补全与复验结论。

### G.1 三条硬检查（决定终判）

| # | 改前 | 改后 | 节号 |
|---|---|---|---|
| **F16** | §7.1 用**两跳**：`insert` 一个 `orchestra-preset-root` group，再往组里 `insert` 一行 `agent-presets` | **单跳 override**：`- id: agent-presets` + `config: {default: standard, roots: [{path: ~/.dsh/orchestra/catalog-presets, trust: system}]}`，**必须自带 `default`**。新增 **§7.4 F16 内存实测记录**：四条探针（A 单跳 ⇒ 一行且 config 正确、0 warning；B 层序对调 ⇒ 覆盖丢失 + `entry not found`；C 两跳 ⇒ **两条同名行**；D 漏 `default` ⇒ `config` 只剩 `roots`）。§7.3-a 的脚本改为**直接调宿主 `composeEntries`**；R-02 改写为"单跳的层序风险" | §7.1 / §7.3 / §7.4 / §8.1-R-02 |
| **F17** | §6 字段表**没有 `expectedNew` 落点** ⇒ 0.3 的幂等恢复无值可读 | 新增记录 **`orchestra/nodes/<node-id>.intent.json`**（`MERGE_INTENT_SCHEMA_VERSION = 1`：`attemptId` / `expectedOld` / `expectedNew` / `previewedTree` / `candidateId` / `at`），**在 `update-ref` 之前落盘**，**独立文件**（不是节点记录的一部分，理由已写）。§4.2 步 6 恢复改成**四段判定**（①== expectedNew ⇒ 补记账、不再 update-ref；②== expectedOld ⇒ 按 intent 重做；③第三个值 ⇒ `base_advanced` 回预演；④已有同 attemptId ⇒ 幂等）。P2-5 行同步为"先落 intent → update-ref → 比对 previewedTree → 记 mergeAttempts" | §6 / §4.2 步 6 / §1 P2-5 |
| **F18** | 0.1 只写"未声明 ⇒ `unbound_input`"，**没写"声明了但内容已变"** | 新增 **§4.3.2 已声明哈希的重算规则**：**宿主**在**每次**验收执行前**逐项重算** sha256；不一致 ⇒ `unqualified` / `input_digest_mismatch`；**不得沿用上次读数、不得先跑再看**；git 跟踪的也重算（清单可声明主 worktree 路径 ⇒ 正是 P7 的入口，不能省）。**§4.3.2 开头明说 F18 与 F20④ 是同一件事的两面**。§4.3 新增变体 **g**、§1 P1-1 与 P1-3 同步 | §4.3 变体 g / §4.3.2 / §1 P1-1 / §1 P1-3 |

### G.2 裁定派生五条（F19–F23）

| # | 改前 | 改后 | 节号 |
|---|---|---|---|
| **F19** | 强制摄入写在 **freeze** 时；"相关缺陷集 + 处置"只在节点记录里 | 时机回到 **准备阶段（prepared）**；并补 **「相关缺陷集 + 每条处置」纳入候选身份的决策快照** ⇒ freeze/merge 侧相关缺陷集一变，**由 A1 自动失效**（`generation + 1`、旧审查旧证据作废），**两侧都覆盖、不加第二个检查点**。§3 的依赖表改为"P3-2 依赖 P2-3（候选身份）" | §1 P3-2 / §3 |
| **F20** | `inputs[]` 只被当作复用判据 | **四条限定全部写入**：①属冻结验收协议（改它 = `generation + 1`）②审查者审"**这个声明是否正确**"③复用时**必须标 `reuseBasis: "declared-inputs-only"` + `completeness: "unproven"`** 并**计入 §1.9⑤** ④**必须覆盖非 git 跟踪输入**（`source: "non-git"`，必带 `reason`）。落点：§0.3 R-1、§6 冻结清单字段表、§6 证据字段表、§1 P2-4、§10.5 A6 行 | §0.3-R-1 / §6 / §1 P2-4 / §10.5 |
| **F21** | §P1-3 有 `extraInputs[]`，§0.3 说 `inputs[]`——**两处各说各话** | **唯一口径：只有 `inputs[]`**（per-check，`{path, sha256, source: "git"｜"non-git", reason?}`）。**`extraInputs[]` 全局删除**。P1-3 / §2.1 `frozen-manifest.ts` / §6 冻结清单 / §9.1 / §10.6 五处一并替换；§6 另写明"清单根级没有第二处输入口径" | §1 P1-3 / §2.1 / §6 / §9.1 / §10.6 |
| **F22** | `reuseScope` 是"**五项闭集**"（读起来像"只看这些"） | 改为**八项闭集**（原五项 + `inputsDigest` + `mode` + `externalDeclaration`），并**写明这是"必须比对的下界"，不是上界**；插件已采集的**其它**身份字段（解释器 / 工具链 / 依赖摘要，落在 `otherIdentity{}`）若不同 ⇒ **`warning` + 计入 §1.9⑤，不得静默放行**。§1 P2-4 补用例⑥ | §0.3-R-2 / §1 P2-4 / §6 / §10.5 A6 / §附-偏离一览 |
| **F23** | §9.1"处置搭在 freeze 上 ⇒ **0 次新增**"，合计 3，§9.2 论证"典型 2 次" | **§9.1 整表重算**：prepared 摄入 **1** + freeze **1** + capsule_submit **1**（+ 请示 **1**）= **典型 3 / 上界 4**；补齐算式与"**为什么 4 不能再压**"（R-4 谓词可判但处置内容是判断；唯一替代"让 driver 携带处置"**不成立**，因为被摄入的是执行节点）。**§9.2 论证同步重写**，§9.3 `defect_disposition` 行改写。另按 F10 补两张表：**审查节点 0 新增**（既有 `orchestra_report` 加字段）、**合并门触发 0**，并给出按节点类型的汇总。新增 **G-BUDGET** 闸 + `verify-agent-budget.mjs` | §9.1 / §9.2 / §9.3 / §11 |

### G.3 其余（F1–F15 + 三处硬伤）

| # | 改前 | 改后 | 节号 |
|---|---|---|---|
| **F1** | `inputs[]` 有值即可复用；变更路径未说明来源 | **`inputs: []`（空数组）一律视为"未声明"** ⇒ 一律判"受影响"逐条重跑；**变更路径由插件 `git diff <C1> <C2> --name-only` 自算，不取自角色**。写进 §0.3-R-1、§1 P2-4、§4.1 步 6′，并加"**空 `inputs[]` 不得出现在任何 `reused` 证据里**"的断言 | §0.3-R-1 / §1 P2-4 / §4.1 步 6′ |
| **F2** | `reuseScope` 五项，**漏模式与外呼宣言**（与 P2-4 用例③自相矛盾） | 加 `mode` 与 `externalDeclaration`（并入 F22 的八项）；P2-4 加**两个直接用例**（④ `externalDeclaration` 变 ⇒ `fresh`；⑤ `mode` 变 ⇒ `fresh`） | §0.3-R-2 / §1 P2-4 |
| **F3** | `unqualified` 是终点 ⇒ **输出不可解析的仓库永远无有效证据且无出口**（活锁） | 新增 **§4.3.1 `unqualified` 的收束出口**：**Ⅰ 可修类** ⇒ 回作者改协议（`generation + 1`，受既有 attempt 上限约束）；**Ⅱ 不可修/未知类** ⇒ **开决策队列记录**（`safetyClass: "non_defaultable"`）⇒ `blocks` 生效、**节点回合正常结束**、**人节点留痕解除**；补"整仓都测不了"的 **`blocked` 终态**（不阻塞其他节点）。新增 `test-unqualified-exit.mjs`（4 用例）；§6 证据字段加 `preconditionClass` | §4.3.1 / §6 / §5.1 |
| **F4** | R-4 的退回理由写作"**不可机械判定**" | 重述为**语义澄清**：原要求本身可机械判定（`dispositions.length === 0 ⇒ 拒`）；真问题是**字面执行退化成填表/活锁**。§0.3 表头与 R-4 行改写 | §0.3 |
| **F5** | 摄入时机从"准备阶段"移到 freeze，**未标注** | 移位**已取消**（回到 prepared，见 F19）；§0.3 补一段"F5 的状态"，说明现在**没有**"不申报候选 ⇒ 永不摄入"的缺口 | §0.3 |
| **F6** | P1-1 写"模型永不提供命令字符串"，与 §9.1/§6"清单含 `command`"**措辞冲突** | §4.1 新增**命令作者表**（prepared 节点提议 → freeze 门冻结并成为记录作者 → 运行期 runner 只从 `frozen.json` 查）；§1 P1-1 与 P2-5 措辞同步；§9.1 的"验收清单"改述为"**验收清单提案**（含 `command`/`env`/`inputs[]`）" | §4.1 / §1 P1-1 / §9.1 |
| **F7** | "谁冻结清单"未定，擦 §0 约束二的边 | **钉死：清单由节点提议，门（插件）冻结并作为记录作者**（同 F6 的表；§0 约束二：会话写意图、插件写事实） | §4.1 |
| **F8** | 预设目录计数 **13** | **12**（实测 `find . -maxdepth 1 -mindepth 1 -type d \| wc -l` → 12）＝ 9 个 v0.4 + 3 个 legacy，并逐个列出；§5.1 与 §7.3-a 的期望改 `healthy=12/12`；§8.1-R-06 同步 | §0.2-B-1 / §5.1 / §7.3 / §8.1-R-06 |
| **F9** | §1 写"`test-orchestra-role-presets.mjs` **已有**该断言" | 实测**无命中**（唯一命中 `test-add-lanes.mjs:343`，测的是工具面不是校验器）⇒ 改为"**将新增**两条断言"并写明"**改前不得写成『已有该断言』**" | §1 E4 |
| **F10** | 附页写"新增**五个** agent 工具"却只点名三个；`orchestra_defect_disposition` 同时被列为独立工具又被说成"并入 freeze" | 改为**四个**并与 §9.1 逐行对齐（`defect_disposition` 因 F19 回到独立动作 ⇒ **是**独立工具）；§9.1/§9.2 补**审查节点**（既有 `orchestra_report` 加字段 ⇒ 0 新增，预算核对按 1 计）与**合并门触发**（0，插件判定）两行 | §9.1 / §9.2 / §附-偏离一览 |
| **F11** | §10.6 fault_ref 表缺两个新机制的落点 | 补 **`src/candidate.ts` → P15 + 契约 A1 行**、**`src/baseline.ts` → P8 + 契约 A7 行** | §10.6 |
| **F12** | §5.1 缺 `verify-negative-N6.mjs`（G-P3 点名）与 `test-orchestra-role-presets.mjs`（E4 的闸） | 两行补入 §5.1；另补 `test-orchestra-archive.mjs`（G-P0 第⑥项点名）与 `test-unqualified-exit.mjs` | §5.1 |
| **F13** | B3② 的收窄**看起来像**计划擅自收窄 | §1 P3-3 行补**出处**：`alignment-0.5-position.md` §4 的 **B3 行**（拆两件事：①硬拒 ②只登记）+ **契约 §3-P3 行**（"写者冲突硬拒；声明完备性只登记"）⇒ **不是我方收窄** | §1 P3-3 |
| **F14** | N6 写"胶囊已生成"，像产品保证 | §4.6 正例**显式加第 0 步**（先 `orchestra_capsule_submit` 生成一次），并写明"生成时机属 R-11 兜底项，所以正例自带这一步" | §4.6 |
| **F15** | 包名两处不一致（B-4 说 `dsh-bash-sandbox`；§2.2/R-08 说 `dsh-shell`） | **钉死为 `@deepseek-ai/dsh-shell`**（`lib/types/index.d.ts:24-25` 在那里 `declare module '@deepseek-ai/cordis'` 加 `Context.shell`）——"定义在 `dsh-shell`、提供者是 `dsh-bash-sandbox`"。B-4 补后半句："**若不以宿主身份运行 ⇒ P1 全部证据不合法，必须停下**" | §0.2-B-4 / §2.2 / §8.1-R-08 |

### G.4 顺手核相邻条款的结果（gate 要求的"重点看三处"）

| 核对项 | 结果 |
|---|---|
| ① `inputs[]` / `extraInputs[]`（F21） | 全部 5 处落点已统一为单字段；残留的两处 `extraInputs` 字样**是刻意保留的**（一处写"**已全局删除**"、一处写"**没有 `extraInputs`**"），用于防止读者再把它当第二口径 |
| ② `inputs[]` 非 git 输入（F20④）与 F18 哈希重算 | **已写在一起**：§4.3.2 开头明说"F20④ 管声明面要盖住非 git 输入、F18 管声明之后内容变了怎么办，两者合起来才是『按哈希显式绑定』的完整语义"，并给一张六行规则表（谁重算 / 何时重算 / 不一致怎么办 / git 跟踪的也要重算 / 非 git 必带 reason / 未声明与已变更用**不同**原因码） |
| ③ 摄入时机改动是否连带改了 §9.1 / §9.2 / §11 | **三处全部连带改完**：§9.1 整表重算 + 新增"F10 两处漏算"表 + 按节点类型汇总；§9.2 论证重写为两句；§11 新增 **G-BUDGET** 闸与 `verify-agent-budget.mjs` |

### G.5 本轮失分类型的自查（内部矛盾 / 声明不实）

逐条重扫了 gate 点名的四类，并额外做了一次全文件脚本化一致性检查（脚本：跨全文收集脚本名 → 与 §5.1 清单求差集；收集闸里点名的脚本 → 逐条核是否在清单里；收集工具名；扫"五项闭集 / 13/13 / 已有该断言 / 五个 agent 工具"等**陈旧措辞**的残留计数）：

| 自查项 | 结果 |
|---|---|
| 13 ≠ 12 | ✅ 已改；全文 `13/13` 与 `13 个目录` 残留计数 **0** |
| 声称测试已有断言却 grep 不到 | ✅ 已改为"将新增"；这是本轮**唯一**一处该类问题，且已在同一句里写明实测命令与命中位置 |
| `reuseScope` 与用例③打架 | ✅ 已修：补 `mode` + `externalDeclaration`（F2），把用例③的意图变成显式用例④⑤；八项**下界非上界**（F22） |
| P1-1 与 §9.1 冲突 | ✅ 已修：§4.1 新增命令作者表（F6/F7），P1-1 与 §9.1 的措辞统一 |
| **闸点名的脚本不在交付清单**（新发现） | ✅ 已修（F12）：`verify-negative-N6.mjs`、`test-orchestra-role-presets.mjs`、`test-orchestra-archive.mjs`、`test-unqualified-exit.mjs`、`verify-agent-budget.mjs` 全部补入 §5.1。**计数更正（第 2 轮 H-6）**：我原写"16 个"，门实测"17 个"——**两个数都不该出现**，因为它们来自同一批歧义 token（`verify-negative-N1/N2/N4/N5.mjs` 这种斜杠组、`--self-test` 这种同脚本的校准模式、以及把 `N1/N2/N4/N5` 展开算 4 个还是 1 个）。现已改为**§11 的显式枚举表**：**20 个具名脚本文件 + `verify-negative-N1..N6` 共 6 个 = 26 个脚本文件**（对应 28 条 `node` 命令，含 2 个 `--self-test` 校准模式）。机械复核：**26 个全部在 §5.1 清单里，差集为空** |
| **"五个 agent 工具"与点名三个不符**（gate F10） | ✅ 已修为四个并与 §9.1 逐行对齐 |



---

## §H 第 3 轮：增量对照表（**只改 6 处**）

> 依据：计划门第 2 轮终判（`docs/gate-round2-fixes.md`）。**21 项 PASS**，三条硬检查 **0.1 / 0.2 / 0.3 全部转正**。本轮**只动** H-1…H-6 六处，其余一字未改。
> 全部改动仍在 `docs/plan-0.8.0-execution.md`；行号为改后行号。

| # | 改前（原文） | 改后 | 行号 |
|---|---|---|---|
| **H-1** | §4.2 步 6 的**四段判定**，②（`ref == expectedOld` ⇒ 按 intent 重做）与④（已有同 `attemptId` ⇒ 幂等）**冲突且无优先级**；"已有同 attemptId"写在最后一段，读者会先命中② | **抽出 §4.2.1「F17 恢复判定表（有序、穷尽）」**：**判定顺序 = 行序**，命中即停；**① `mergeAttempts[]` 已有同 `attemptId` ⇒ 已完成（优先于 ②④，是冲突的裁决者）**，若 ref 与 `expectedNew` 不符 ⇒ 记 `ledger_ref_mismatch` 进立即阻断清单；② `ref == expectedNew` ⇒ 补记账、**不再 update-ref**；③ `ref == expectedOld` ⇒ 按 intent 重做；④ 第三个值 ⇒ `base_advanced` 回预演；**⑤ intent 不可解析 ⇒ 视为无 intent**；⑥ 无 intent ⇒ 无动作。**表末行为"其它一切"，故穷尽**。§4.2 步 6 改为指向 §4.2.1 的指针 | §4.2 步 6 / **§4.2.1（新增）** |
| **H-2** | 全文 **0 处** `rename` / `fsync` / 临时文件，无"intent 落盘中途崩溃"的规则 | §4.2.1 补**写入原子性**：**临时文件 `<name>.intent.json.tmp.<pid>` → `fsync` → 原子 `rename`**；因此"半份文件"只可能出现在 `rename` 之前，而它**不叫 `intent.json`** ⇒ §6 的落点只会看到完整内容。**解析失败一律按"无 intent"处理（表第 ⑤ 行），绝不按半份内容继续**——猜崩溃点的语义正是 0.3 禁的东西。`test-merge-cas.mjs` 增加对 `rename` 的 **grep 断言**（防止有人改回直接 `writeFile`） | §4.2.1 |
| **H-3** | §0.2-B-2 括注：「（**§7 给出两跳写法**与回退写法）」 | 「（`- id: agent-presets` + `config` 自带 `default` 的**单跳 override**；§7.1 给正文、§7.4 给内存实测、§8.1-R-02 给回退写法）」 | §0.2-B-2 |
| **H-4** | 附录「偏离一览」：「§7 部署改为 profile patch 层**两跳写法**」 | 「§7 部署改为 profile patch 层的**单跳 override**（`- id: agent-presets` + `config` 自带 `default`）」，并补一句内存实测结论（单跳 ⇒ 恰一行 0 warning；两跳 ⇒ 两行同名，**已不采用**） | 附录·偏离一览 |
| **H-5** | §9.1 写「在**"审查节点合计"里报 1 次**」× §11 写「审查型 **0 新增**」——**两个口径混用**；且 `verify-agent-budget.mjs` 只有 `expect<=4`，对"审查型合计 1"**恒真**，**判不出 0 新增** | §9.1 新增**「两个口径」表**：**新增（new）** = 相对基线 `6bd8d9b` 的**额外**动作数（判据 9 按此衡量）；**合计（total）** = 该节点本次**全部**动作数。汇总表**两个数字都列**（交付型 new 3/4 = total 3/4；**审查型 new 0 / total 1**；driver new 0；档 0 total 0）。§11 的 G-BUDGET 与 §5.1 的脚本行改为**逐项分开断言**：`delivery_new ≤ 3`（真实请示时 4）且 `new == total`、**`review_new == 0`（硬断言）**、`review_total == 1`、`driver_new == 0`、`tier0_total == 0` | §9.1 / §11 G-BUDGET / §5.1 |
| **H-6** | 本文件 §G.5 写「闸里点名的 **16** 个脚本 100% 在清单里」，门实测 **17** | **两个数字都从正文删除**，因为歧义 token（斜杠组 / `--self-test` / `N1/N2/N4/N5` 展开）让"数到几"本身不可靠。§11 新增**显式枚举表**（逐闸逐列）：**20 个具名脚本文件 + `verify-negative-N1..N6` 共 6 个 = 26 个脚本文件 / 28 条 `node` 命令**；机械复核**差集为空**。§5.1 的 `verify-negative-N1..N5` 缩写行改成**6 个独立脚本的完整具名行**，删掉重复的 N6 单行 | §11（新增枚举表）/ §5.1 / 本文件 §G.5 |

### H.1 F17 四段表是否真穷尽（门第 3 轮只验这一项）

**旧表的漏洞**：四条断言的前提互相重叠——"`ref == expectedOld`"与"已有同 attemptId"可以同时为真，而两段给出的动作**相反**（重做 vs. 什么都不做）。`ref == expectedNew` 与"已有同 attemptId"同样可以同时为真。

**新表为什么穷尽**：

1. **判定按行序，命中即停**，所以重叠不再是歧义，而是**优先级**问题——`attemptId` 已记账是终态事实，**优先于任何 ref 观察**，因此它排在第 ① 行。
2. **前四行按 `ref` 的取值划分为互斥且穷尽的四类**：`== expectedNew` / `== expectedOld` / 第三个值 / （在 ① 已排除"已记账"的前提下，这三种覆盖全部可能）。
3. **第 ⑤⑥ 行按 `intent` 的可读性补全**：可解析但不在前四类 ⇒ 已被前三类覆盖；不可解析 ⇒ ⑤；不存在 ⇒ ⑥。**没有任何输入会落到表外**。
4. **写入原子性（H-2）保证 ⑤ 是可达但稀有的分支**——`rename` 之后不可能出现半份文件，所以 ⑤ 只覆盖"`rename` 之前就被中止"这一种情形，而且**它明确不猜**。

**判定脚本必须覆盖六行各一个用例**（写进 §4.2.1）：
- **① vs ③ 的重叠用例**：`ref == expectedOld` **且**已有同 attemptId ⇒ **必须走 ①**（记 `ledger_ref_mismatch`、**ref 一动不动**），**不得**走 ③ 重做。这是 H-1 的直接回归测试。
- **⑤ 截断用例**：在 `intent.json` 里写半份 JSON ⇒ 走 ⑤、不崩、**不按半份内容继续**。
- **⑥ 正常路径**：无 intent ⇒ 无动作。
- 另加 `rename` 的 grep 断言（H-2）。

### H.2 本门两条裁定（记录在案，后续不得再翻）

1. **agent 侧预算接受为「典型 ≤3 / 上界 4」**：第 4 次触点**仅出现在确有真实请示时**；判据 9 的"≤3"按**典型值**理解。⇒ §9.1 / §9.2 / §11 G-BUDGET 的现有写法**保留**（本轮**未**因为这条改动预算结论，只按 H-5 补了两个口径的区分）。
2. **§3.3-2 字面"断言它不发起审批"判定为做不到，接受可判定弱形态**（拒绝即时 + `asked/decided` 成对 + 回合有 `turn/end`）；**round-8 对方已回"正是我们要的"** ⇒ **视为双方已同意**，文档如实标注"对需求字面的弱化"即可，**不再回问**。落点见 §4.8 末段与 §G.5 的"做不到"表。

---

## §I 第 4 轮：Pro 二审后的增量对照表（P-1…P-12）

> 依据：`docs/gate-round3-pro-findings.md`（Pro 第 2 轮复审，基线 `main@55d4352`）。**本门上一轮 PASS 已撤回**。
> 顺序按门的要求：**P-1 → P-7 → P-2/P-3/P-4/P-5 → 其余**。
> 本轮**动了两个文件**：`docs/plan-0.8.0-execution.md`（主体）与 **`docs/plan-0.8.0-delivery-layer.md`（契约 §1.2 档 0 行，v1.2 → v1.2.1）**——**P-1 必须动契约**，因为计划 §0.3 的旧文本写的是"契约 §1.2 档 0 的机械谓词里……**本计划把它实现为……**"，即**谓词的定义在契约里、实现在计划里**；只改计划而留契约原文，就会留下第二处口径。**这是本轮唯一一处契约改动，且是收紧 + 澄清。**

### I.1 P-1（最高优先·弹性）

| 改前 | 改后 | 落点 |
|---|---|---|
| **契约 §1.2 档 0 行**："机械谓词全部满足：**执行会话 = 1**、交接 = 0、依赖边 = 0、输出仅聊天、合并/发布/可复用 PASS 动作全部禁用" | "机械谓词全部满足：**无任何交付动作**（合并 / 发布 / 可复用 PASS 全禁用）、**交付型交接 = 0**、**交付型依赖边 = 0**、无插件可见的产物引用。**不限制聊天会话数**，**不限制纯聊天会话的交接与依赖**" | 契约 §1.2（**v1.2.1**） |
| 计划只在草稿边界里提了一句"依赖边 = 0 与禁用动作**实现为准入声明 + 插件重算**"，**没有规范定义**，也没有回答"两个纯聊天会话怎么办" | 新增 **§0.4「档 0 谓词的实现口径」**（五条）：**① 不限制聊天会话数**；**② 交接 / 依赖只统计"交付型"**；**③ 对全部交付动作的硬禁保留**；**④ 标准档默认只作用于「已准入的交付节点」**（没有交付动作就不进交付层：零节点记录 / 零摄入 / 零胶囊 / 零租约）；**⑤ 准入判定只看插件可见的事实**（交付动作 / 产物引用 / 依赖），**不采信 driver 的"不交付"标签**；并写明"宿主外的自主写文件 / 合并**不阻止也不认**，不得包装成已治理交付"（`capability-boundaries.md` #7） | 计划 **§0.4（新增）** |
| P3-6 的消息指针纪律管**所有**消息 | **只管交付消息**（涉 nodeId / decisionId / 证据 / 合并 / 释放的那些）；**纯聊天消息不受约束**——否则档 0 的零成本会被一条消息格式规则吃掉 | 计划 §0.4 / §1 P3-6 |
| 无判定脚本 | 新增 `scripts/test-tier0-predicate.mjs` + `verification/cases/tier0-predicate.cases.json`，**五个用例**：①两个纯聊天会话都判档 0 且零交付物 ②其中一条要合并 ⇒ 只它进重新准入、另一条仍档 0 ③**重新准入不追认此前的 PASS**、权限与外呼审批不随豁免关闭 ④**driver 说"不交付"但插件看到交付动作 ⇒ 仍按交付型** ⑤**driver 说"交付"但插件看不到任何交付动作 / 产物引用 / 依赖 ⇒ 判档 0**（不能靠标签把成本加回来） | 计划 §0.4 / §5.1 |

### I.2 P-7（0.3 的实质缺口）

| 项 | 改前 | 改后 | 落点 |
|---|---|---|---|
| **P-7(i)** | **四段表**，且"⑤ 不可解析"排在**末行**，但①②③④ **先读** `intent.expectedNew/expectedOld` —— **先用无效输入、最后才处理无效输入** | **两段式**：**阶段 A 先判 `intent` 的形态**（A1 不存在 / A2 不可解析 / A3 合法，命中即停），**阶段 B 才读字段** | §4.2.1 |
| **P-7(ii)** | 第 ① 行"`mergeAttempts[]` 里有同 `attemptId` 的条目 ⇒ 已完成"，**不区分它是不是成功终态记录** | 拆成 **B1**（有同 attemptId **且 `result == "merged"`** ⇒ 已完成；**再核账实**：`ref == expectedNew` ⇒ 正常；`ref` 是其后继 ⇒ **合法后继、不报账实不符**；非后继 ⇒ `ledger_ref_mismatch` 进阻断清单；**判不出 ⇒ 阻断**）与 **B2**（有同 attemptId 但 **`result != "merged"`** ⇒ **不是成功终态**，继续走 B3–B5——上一版会把这种记录也当"已完成"，**永远不再尝试合并 = 活锁**） | §4.2.1 |
| **P-7(ii-b)** | **B5**："`ref` 是第三个值 ⇒ `base_advanced` 回预演" | 拆成 **B5**（第三值**且 `candidateCommit` 是 `ref` 的祖先** ⇒ A **已成功**、其后有人推进 ⇒ **补记账、不重演**）与 **B5′**（第三值但**不是祖先** ⇒ 才判 `base_advanced`）。**必须拆**：否则"A 崩溃 + B 合法推进"会重演 A = 重复合并 | §4.2.1 |
| **P-7(iv)** | 第 ⑥ 行措辞"**末行是"其它一切"**" | ⑥ 只写"**不存在**"；穷尽性改由 **A1/A2/A3 × B1–B6** 的覆盖论证说明（A 覆盖 intent 全部形态；B 在 A3 下按 `ref` 取值分 B3/B4/B5/B5′，互斥且穷尽） | §4.2.1 |
| **P-7(iii)** | 只要求"每行一个用例" | **补两个组合用例**：**组合 1（A 崩溃未记账 + B 合法推进）** ⇒ 走 B5 ⇒ 补 A 的记账（**不是**重演、**不是** `base_advanced`），并断言 `git reflog <ref>` 合计 **2 条**（A 一步、B 一步）；**组合 2（A 已记账 + B 合法推进）** ⇒ 走 B1 ⇒ 认出合法后继 ⇒ **不报账实不符、不补第二条记录** | §4.2.1 |
| §6 的 intent 字段缺祖先判定所需项 | `mergeIntent` 补 **`candidateCommit`**；`mergeAttempts[]` 补 `result` 与 `supersededBy` | §6 |

### I.3 P-2 / P-3 / P-4 / P-5（证据与越界）

| 改前 | 改后 | 落点 |
|---|---|---|
| **P-2**：F22 的"下界非上界"被读成"**可以带着告警复用**"——甚至"工具链已变仍 `reused+warning`" | **撤回该宽松读法，回到契约 §1.4 原文**：**八项里任何一项不同 ⇒ 重跑**；**已采集到的、与验收相关的其它运行条件任何一项不同 ⇒ 重跑**（不是告警）；**"告警 + 计入证据质量"只适用于在冻结协议里显式声明为无关的字段**（新增 `irrelevantConditions[]`）；**未经采集的条件 = 未知依赖 ⇒ 不复用** | §0.3-R-2 / §1 P2-4 / §6 |
| 证据 schema 里 `otherIdentityMismatch?: true` 表示"差异但复用" | 改为 `irrelevantConditions[]` + `irrelevantConditionMismatch?`（**只在命中声明为无关的字段时出现**，属告警）；`otherIdentity` 里任何一项不同 ⇒ **`fresh`** | §6 |
| **P-3**：复用是默认允许，只"标注完备性未证" | **复用从"允许"降回"例外"**：`reuseKind: "exact"｜"reused_unverified_inputs"`（后者带**类型化标记**）+ 计入 §1.9⑤ + **报告里单列计数 `unverifiedInputReuseCount`** | §6 / §1 P2-4 / §1.9⑤（经 F 轮已并入的字段表） |
| **P-4**：跑前重算哈希 | **不够**——它只证明"读到的与声明一致"，**不证明"跑的就是那一份"**（TOCTOU）。收口：**freeze 时物化「绑定副本」**到 `orchestra/nodes/<node-id>.inputs/<check-id>/…`（复制时算 sha256）⇒ **每次执行前复核副本**（F18 规则不变、对象变副本）⇒ **验收一律从副本读取**，映射方式由 `inputs[].materializedAt` 显式记录；**主 worktree 的活路径不再作为验收输入来源**。证据加 `inputsReadFrom: "bound-copy"` | §4.3.2 |
| **P-5**：对账只看 `git status --porcelain` | **会漏掉已提交的越界改动**（`status` 相对 HEAD，提交后就干净了）。改为**基线 → 候选的差集**：①`git diff --name-only <baseCommit> <candidateCommit>`（**`baseCommit` 在准入时 `rev-parse` 并写进节点记录**）②`git status --porcelain -uall` ③合并去重后比对声明写入面；两边都只用 git 自己的输出，**不遍历文件**；`baselineRef` 缺失 ⇒ **拒绝判定**（`baseline_unknown`），不猜 | §1 P2-7 / §3 依赖表 / §6 |
| 对账测试 3 个用例 | **6 个**，新增：④**越界改动已 commit 进候选分支 ⇒ 仍必须被点出**（P-5 核心回归，只用 `status` 时是**假通过**）⑤未提交的越界同样被点出⑥`baselineRef` 缺失 ⇒ `baseline_unknown` | §1 P2-7 |

### I.4 其余（P-6 / P-8 / P-9 / P-10 / P-11 / P-12）

| # | 改前 | 改后 | 落点 |
|---|---|---|---|
| **P-6** | §1.3 的"不交新改动 ⇒ 仍交旧候选 / 必须交 ⇒ 才要新审查"**只在正文，没有可执行用例** | 补**三个用例**：**P6-a** 分支上有新 commit 但**未申报** ⇒ 冻结的仍是 `C1`、合并门只认 `C1`、不触发重审（**不得把分支 HEAD 静默当新候选**）；**P6-b** 显式申报 `C2` ⇒ `generation + 1`、`stale_candidate_review`；**P6-c** 未申报但新 commit 改了冻结清单里的 `inputs[]` 路径 ⇒ **判定仍以 `C1` 的冻结清单为准**，同时**收工对账（P-5）会把它的路径纳入差集**，越界则按 P2-7 处理——**两条路径各管各的，不互相冒充** | §4.1 |
| **P-8** | 预算写成"典型 3 / 上界 4"，把**一轮成功路径**当成了全部（**返修再冻结会出现第 4 次；多次请示会超 4**） | 计量单位改为**单轮**：`per_round.delivery.new == 3 + 请示数`，且 `== total`；**生命周期新增 = 各轮之和，含返修，只累加、不设上界**；**额外槽必须绑定真实决策记录**，且"""记录存在"不证明"请示必要"**（必要性交 P-11 的每周复查抽样）；`total` 明确**含**基线已有的报告与通信（给理由）。G-BUDGET 判据整块改写 | §9.1 / §11 |
| **P-9** | 决策记录字段表缺四项；`DECISION_SCHEMA_VERSION = 1` | 补 **①`revocation{how,by,at?}`**（撤销方式）**②`costCap{unit,limit}`**（成本上限）**③`downstreamMark`**（下游"尚未批准"标记，依赖该默认值的产物**必须**带 `provisional: true`）**④`refusalTest`**（对应的拒绝测试）；**版本升到 2**（新增必填字段 = 版本递增）。`test-decision-queue.mjs` 补四项用例 | §6 / §5.1 |
| **P-10** | 可验证面**散落正文**：§5.1 / §11 手写表、§6 手写字段表、三处表靠人肉对应；且"grep 命中即算证明" | 新增 **§5.0**：**三个机器可读源 + 一个生成器**——**①`verification/manifest.json`**（唯一结构化清单：`{gate, script, args[], cwd, env[], expectedExitCode, asserts[], dependsOn[]}`；**§5.1/§11 的表由它生成**）**②`verification/schemas/*.schema.json`**（**§6 字段表由它生成，插件写侧用同一份校验**）**③`verification/cases/*.cases.json`**（**表驱动**：§4.2.1 / §0.4 / §9.1 三处的**行 == 用例**，行数不匹配即失败）。正文只留**顺序 / 理由 / 边界 / 测试引用**。新增 `scripts/{run-cases,gen-verification-docs,verify-all}.mjs` | §5.0（新增） |
| **P-10（附）** | grep 断言被当成行为证明 | **grep 断言普查**：本文 6 处，**2 处有行为配对、4 处是纯结构性守卫**，后 4 处**明确标注「结构性守卫，不承担行为证明」**并各自指名承担行为证明的用例。**grep 只保留一个用途：防回归** | §5.0 |
| **P-11** | 每周复查**只能报警**，不能真的执行删除 | 按机制记录**五样**：实际成本 / **独有拦截证据** / **等强替代测试** / 执行人 + 截止窗 / **实际停用结果**。**四条硬规则**：①缺覆盖证据 ⇒ `pending_verification`，**不算"无候选"**②**到期未执行 ⇒ 不得记复查完成**（停 `overdue`）③**没有等强替代 ⇒ 保留**（"最近没出事"不是删除理由）④**停用结果必须回填**，为空 ⇒ 仍在册。测试补四条 | §1 P4-2 / §5.1 |
| **P-12** | §11 G-P0 允许 `--self-test` **退出 1**，同处却写"**任一非 0 即不进 P1**" —— **自相矛盾** | 明确**退出码口径**：本闸的"通过" = **每个脚本的每个模式各自命中它自己的期望码**（正常 0 / 校准 1），**不是"所有命令都返回 0"**；结论列改为"任一项**未命中其期望码**即不进 P1"。并由 `scripts/verify-all.mjs` 按 `manifest.json` **逐项核对期望码**（机械保证，不靠人读） | §11 G-P0 |

### I.5 0.1–0.3 复验（P-7 决定 0.3）

| 硬检查 | 结论 | 依据 |
|---|---|---|
| **0.1 冻结快照执行** | **维持 PASS**（本轮**加强**） | P-4 补掉了"跑前哈希"与"执行读取"之间的 **TOCTOU**：改为 **freeze 时物化绑定副本 + 执行前复核副本 + 验收从副本读**，并记 `inputsReadFrom: "bound-copy"`。P-2 把复用判据收回到"任何已采集的相关条件不同 ⇒ 重跑"，未知依赖不复用 |
| **0.2 活跃租约冲突阻断** | **维持 PASS**（本轮**加强**） | P-5 把对账的检出面从 `git status` 扩到 **基线→候选差集**，堵住"**已提交的**越界改动"这条漏检；越界命中活跃租约仍**阻断冻结/合并**、豁免不取消 |
| **0.3 `update-ref` + 崩溃幂等恢复** | **本轮申请转 PASS** | P-7 的四项修正全部落地：**(i)** 两段式（先判 intent 形态，再读字段）；**(ii)** B1 只认**成功终态记录**、B2 把非成功终态继续往下判（堵活锁）、B1 的账实核对三分支（相等 / 合法后继 / 非后继 / 判不出⇒阻断）；**(iii)** 补「A 崩溃 + B 合法推进」与「A 已记账 + B 合法推进」**两个组合用例**；**(iv)** ⑥ 只写"不存在"，穷尽性由覆盖论证给出；另拆出 B5 / B5′，消除"把已成功的 A 重演一遍"的重复合并 |

### I.6 本轮的分工与自查

- **动了契约一个文件**（§1.2 档 0 行，v1.2 → v1.2.1）——已在 §I.1 说明为什么**必须**动：谓词定义在契约、实现在计划，只改一边等于留第二处口径。**建议按门的要求发给 UCBIP 确认（P-1 与 P-2 都是收紧、不是新增机制）**，但**不等**。
- **迟到的自我纠正**（诚实标注）：本门上一轮说"失分类型是本轮引入的新矛盾"，Pro 指出**不准确**——**F17/F18 是语义缺口**，不只是文字不同步。本轮 P-7 与 P-4/P-5 正是那类缺口的收口。
- 逐条重扫了"改一处、打脸相邻条款"：P-1 → §0.4 / P3-6 / 契约 §1.2 / 新增用例；P-5 → §3 依赖表 / §6 `baseCommit` / 对账用例；P-7 → §6 intent + mergeAttempts 字段 / §4.2 步 6 指针 / 组合用例；P-8 → §9.1 / §11 G-BUDGET；P-10 → §5.0 / §5.1 / §11 / §6；P-12 → §11 G-P0。

---

## §J 第 5 轮：P-8 / P-10 的收尾 + 5 处新矛盾清理（R3-1…R3-5）

> 依据：计划门第 4 轮复验（**P-1…P-7、P-9、P-11、P-12 全 PASS，0.3 因此转 PASS**；门判 **PASS**）。本节只落 **P-8、P-10 两个 PARTIAL + 5 处新矛盾**。
> 改动落在 `docs/plan-0.8.0-execution.md` 与 `docs/plan-0.8.0-delivery-layer.md`（后者仅 R3-4 的一处交叉引用）。

| # | 改前 | 改后 | 落点 |
|---|---|---|---|
| **R3-1** | 同一份计划里**两处判据打架**：`:887` `per_round.delivery.new == 3 + decisions` vs `:935` `delivery.new <= 3`（**确有真实请示时允许 4**），`:707`、`:1076` 同。**`decisions ≥ 2` 时前者得 5、后者判失败** | **统一为 `== 3 + decisions`**，`≤3 / ≤4` 的写法**全文删除**。落点：**§9.1 的代码块成为全文唯一的判据定义**（其余两处改为**显式引用 §9.1**，并各自写明"此处只引用，不另立定义"），§5.1 的脚本行与 §11 的 G-BUDGET 行同步；并写明 `decisions` **只计绑定真实决策记录的请示**（`extra_slots.every(d => d.decisionId != null)`），**"记录存在"仍不证明"请示必要"**（必要性交 P-11 抽样） | §9.1 / §9.2 / §5.1 / §11 |
| **R3-2** | `:104` 说"消息指针纪律**只管交付消息**" vs `:180` 说"**协作消息**只承载指针与唤醒" —— **`协作消息` 的范围没写** | §0.4 该段补一句把它们**显式同源**：纪律里的"消息"一律读作"**交付语境下的协作消息**"；而"**任何正确性条件不得依赖消息成功送达**"是**全局**的（对纯聊天消息同样成立）。§1 P3-6 行改写适用范围并**加第三个用例**：纯聊天消息含自由文本 ⇒ **不失败**（R3-2 的边界用例） | §0.4 / §1 P3-6 / §5.1（`test-pointer-messages.mjs`） |
| **R3-3** | `:499` 要求 `inputs[].materializedAt`，`:724` 的 schema 只有 `{path, sha256, source, reason?}` ⇒ **字段缺失** | 冻结清单 schema 的 `inputs[]` 项改为 `{path, sha256, source, reason?, materializedAt}`，并写明它 = 该输入在**绑定副本**里的落点路径、由 **P-4** 在 freeze 物化时写入；**同一份 schema 插件写侧也用于校验**（P-10 ②） | §6 冻结清单 |
| **R3-4** | 契约 `:3` 指向 `docs/plan-0.8.0-execution.md` 的 **§1.0** —— **该章节不存在**（计划里是 §0.4） | 契约改为指向 **§0.4** | 契约 `plan-0.8.0-delivery-layer.md` |
| **R3-5(a)** | `:446` "条目数 = 1" vs `:436` "合计 2 条" —— 读起来像互相矛盾 | **各自给语境**：`:446`（§4.2 步 7）标注为**单节点语境**（只有 A 在操作该 ref，`reflog` **条目数 = 1**）；`:436`（组合用例 1）标注为**双节点语境**（A 一步 + B 一步，**合计 2 条**）。两处都加一句"**两个数字都对、各管各的语境，不可互相套用**" | §4.2 步 7 / §4.2.1 |
| **R3-5(b)** | §5.0 的三类 `verification/` 源写成**现在时**（读起来像已存在） | 表头改「文件（**将产出**）」、三行的文件名后各加「（**将产出**）」、表前加措辞纪律段（"**目前都不存在**，是本计划将产出的交付物；落地前 §5.1/§11/§6 的表仍是手写的，同时是生成器的输入规格"）、表后"新增文件"改"**将产出的文件**"并补"**当前一个都不在磁盘上**"。**依据：需求 §4 约束 7（未验证的不要写成现状）** | §5.0 / §2.1 |

### J.1 门点名的三条 grep 复验（已自跑）

| grep | 结果 |
|---|---|
| **预算判据唯一** | `== 3 + decisions` 全文 **3 处提及，其中 `per_round.delivery.new == 3 + decisions` 形式的**定义只有 1 处**（§9.1 代码块，L884）；另两处（L709 §5.1、L1070 §11）已改成**显式引用 §9.1** 并写明"不另立定义"。`<=3 / <=4 / ≤3 / ≤4` 的**活判据 0 处**（残留的 4 处全部是"已删除/不再采用/原写法"的历史说明） |
| **schema 含 `materializedAt`** | 冻结清单 schema 的 `inputs[]` 项含 `materializedAt`（§6）；P-4 的"验收读副本"一节也要求由该字段记录映射方式（两处一致） |
| **§1.0 → §0.4** | 契约里 `§1.0` 出现 **0** 次、`§0.4` **1** 次；计划里存在真实的 `### 0.4 P-1` 标题，且无 §1.0 标题 |

### J.2 顺带自查（本轮新矛盾类型：同一数字/判据两处表述）

R3-1 与 R3-5(a) 是同一类问题的两个实例（**同一件事两处表述**），所以顺手做了一次全文件扫描：除上述两处外，还发现 **§9.2 里残留了一份与 §9.1 几乎相同的判据代码块**——那是 H-5 与 P-8 两轮改动叠加的产物。已**删除该重复块**，§9.2 只保留"为什么必须逐项分开断言"的理由并指向 §9.1。**现在全文的判据定义各只有一处**：预算在 §9.1、恢复在 §4.2.1、档 0 谓词在 §0.4。
