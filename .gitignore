# --- 依赖与构建产物 ---
node_modules/
.pnpm-store/
lib/
*.tgz

# --- 锁文件（本地开发用；仓库不锁依赖） ---
pnpm-lock.yaml
package-lock.json

# --- 运行时 / 隐私数据（绝不公开：含会话 ID、绝对路径、团队实例记录） ---
orchestra/

# --- 内部开发文档（不随公开仓库发布） ---
AGENTS.md
STATE.md
DSH-INTEGRATION.md
TESTING.md

# --- 顶层早期布局残留（插件实际读取 <cwd>/orchestra/... ；顶层这几目录是旧结构，不入库） ---
archive/
state/
reports/

# --- 未跟踪用户资产（永不提交、永不删除；规则来源 STATE.md §5 与 reports/E2E-TESTING-HANDBOOK.md §9） ---
# external report/  = v0.4「协作宪章」设计原委的外部咨询稿（GPT / Gemini）
# specs/            = orchestra 自身跑出的 mission spec 样例产物，非插件源码
# test-bug1-*.mjs   = v0.3 期 Bug #1 回归套（v0.4 行为变更后 2 项失败属预期，正式基线以 npm test 为准）
/external report/
/specs/
/scripts/test-bug1-cross-cwd.mjs

# --- 本机 / 临时文件 ---
.DS_Store
*.log
.env
.env.*
probe.txt
.probe-mkdtemp.txt