# orchestra-dsh · 需求文档（迭代中）

> 本文件是 orchestra-dsh 的需求与交互设计蓝图，持续迭代。配套阅读：`README.md`（摘要与设计决策；安装/构建指引见 README「快速开始」）。安装防崩的内部开发手册不随公开仓库发布。
> 写作顺序约定：**先定义用户可感知的流程（自然语言能描述的场景），再反推工具契约与数据模型**。未定事项进「开放问题」。
>
> **v2 修订（2026-08-16）**：确立**对话式动态编排（`/team`）为主路径**，模板降级为可选预设；新增**角色自我认知协议**（底层关键）。

## 0. 产品定位

orchestra-dsh 是**轻量的多智能体编排脚手架**——本质上是一个简单形态的 SAG（multi-agent 编排）系统，**不应该复杂**：

- **主路径：对话式动态编排**。用户一句话（`/team 我想把 A 模块实现出来`），orchestrator 与用户沟通 1~2 轮，然后**自行决定角色集、创建角色会话、派活、回收、交付**。不依赖预定义模板。
- **模板 = 可选预设**。想省事就用现成模板开局；想自由就动态编排。模板机制做扎实的前提是底层（动态编排 + 角色认知）扎实，**模板本身不是关键**。
- **角色 = 可观察的会话**。角色是人类可观察的会话（可看进度、可看产出），不是用完即弃的 subagent；适合严肃/超长任务：各角色持有独立上下文，避免单会话反复压缩。
- 插件只提供架构与自动化；**编排决策（角色怎么组、任务怎么分）属于 orchestrator 与用户**。

### 0.1 差异化优势：A2A 消灭 plan mode（2026-08-16 用户洞察）

传统 agent 的 plan mode 是**同一上下文内的模式切换**（计划 → 执行），代价是上下文膨胀、计划与执行纠缠。orchestra 用 A2A 把它整个干掉：

- **讨论即计划**：用户（或 orchestrator）直接与一个深度思考角色（如 oracle/architect）**对话式推演**——你来我往的 a2a 消息就是完整的计划过程，可观察、可回放；
- **计划即任务**：推演收敛后，把讨论结论**原样作为自包含任务**派发给执行角色开干——不需要任何"模式切换"；
- **计划与执行上下文彻底分离**：思考会话（计划）与执行会话（干活）各持独立干净上下文，互不污染；
- 这是本插件区别于"单会话 + plan mode"工作流的核心卖点，宣传与演示时优先讲这个故事。

## 1. 交互原则（跨 UI 通用）

### 1.1 原则一：回复消息模式是唯一承诺的反馈通道

所有交互的结果都组织为**一条自然语言回复**（由 agent 基于工具返回的结构化数据组织），用户在 WebUI / TUI / GUI / 任何调用方看到一致的对话流体验。

- 工具契约只承诺：结构化数据 + 组织回复所需的信息。不承诺任何 UI 元素、面板、弹窗。
- 判断标准：任何交互在"纯文本对话流"里都必须完整可完成。UI 只是加速器，不是必需品。

### 1.2 原则二：工具只输出数据，文案由 agent 组织

- 工具不返回预设话术，返回结构化事实；agent 负责语气、取舍、引导。
- render 展示函数只做兜底摘要，不替代 agent 回复。

### 1.3 原则三：UI 增强是远期可选项

- Web 设置页可视化编辑模板（拖拽角色到工作区）属于 UI 增强，不改变工具契约与数据模型。

### 1.4 原则四：入口形态等价（斜杠 / 自然语言 / 工具）

- `/team` 斜杠命令、自然语言（"我们开个团队做 X"）、工具调用（orchestra_create 等）是**三种等价的入口**，触发同一套编排流程。
- 斜杠命令是 DSH 原生的快捷入口；GUI 没有斜杠也不影响——自然语言同样触发。
- 实现注意：编排流程需要 LLM 参与（沟通、决策角色），斜杠命令处理器应把命令体作为"编排任务"注入 orchestrator 的对话流，而不是在命令代码里硬编码编排逻辑（已实现：`commands.register` + handler 注入，`recordInput: false`）。

### 1.5 原则五：工具层语言统一为英文（2026-08-16 定稿）

- 工具 description / render / 错误消息 / systemPrompt section / 命令描述**全部英文**（代码纪律层一致）。
- 模板文案（topologies/*.json 的 name/description/welcome）是**用户资产**，语言由用户自定（当前为中文）。
- 理由：语言混合（英文工具 + 中文 prompt）无设计意图，仅历史遗留（a2a 继承自旧动态插件）；统一英文消除风格漂移。

## 2. 核心场景：`/team` 对话式动态编排

### 2.1 触发与流程

```
用户：/team 我想把 A 模块实现出来
  ↓
[1] orchestrator 与用户沟通 1~2 轮，收集：
    目标与交付物、约束（时间/质量/验收）、需要的角色（或由 orchestrator 建议）
  ↓
[2] orchestrator 决策角色集：
    参考模板（若有匹配的）或动态定义（角色名/职责/模型/沙箱）
  ↓
[3] 创建角色会话（create session），逐个注入「角色自我认知协议」（见 §3）
  ↓
[4] 派活：a2a_send 给每个角色自包含任务（不依赖对话历史）
  ↓
[5] 运行与回收：角色向 orchestrator 回复（a2a_reply），产出经 orchestra_report 落盘；
     orchestrator 汇总，向用户交付
  ↓
[6] 结案（或留着持续协作）：orchestra_dismiss 归档实例
```

### 2.2 启动协议（固定 6 步，2026-08-16 定稿）

`/team` 后的引导流程是**固定的**（模型可穷举的确定性步骤），第 4-5 步**不可跳过**：

| 步 | 动作 | 说明 |
|---|---|---|
| 1 | 引导目标（goal） | 一组拓扑完整完成的就是一个 goal；目标明确 → 知道何时停止 |
| 2 | 问限制与约束 | 时间/质量/边界；能合并进同一轮就合并 |
| 3 | 读上下文 | 根据目标读项目相关文件/文档 |
| 4 | **汇报拓扑方案** | 对任务的理解 + 要开哪几个角色、每个角色干什么、为什么这么设 |
| 5 | **等用户放行** | 用户明确批准前**不得创建任何角色会话** |
| 6 | 执行 | create/spawn 角色 → a2a_send 派发自包含任务 |

（旧版"沟通 1~2 轮、信息足够直接开始"已废弃——信息足够只是跳过步骤 1-2，步骤 4-5 永远是必须的。）

### 2.3 确定性路由（协议软化，2026-08-16 定稿）

从中心化到非中心化，解放的是 **driver 的上下文**——它不做信息中枢，只做决策者。要消灭的是**无意义路由**（driver 不经思考的普通分发）：

- **固定 flow 直连**：执行链内的消息在角色间直接流动，永不经过 driver。例：implementer 完成 → 直接交 reviewer；reviewer 发现问题 → 直接发回 implementer 修；定向复查通过 → reviewer 通知 driver"推进下一步"；
- **只上报决策点**：driver 只接收需要它判断的事——推进、阻塞、新方向、跨链协调；
- **默认回退**：任何不确定的消息一律发给 driver（模型永远不困惑，确定性优先于自由裁量）；
- **核心角色命名**：driver（驾驶员）——按目标启动团队、拥有决策的角色；旧称 orchestrator，技术标识 orchestra-manager。

### 2.4 角色集决策

- orchestrator 决定角色集：可以来自模板（见 §4），可以动态定义。
- 动态定义的最小字段：角色名 + 职责（一段话）+ 可选的 preset/模型/沙箱。
- 决策要向用户透明：创建前用一句话说明"我打算建 X、Y 两个角色：X 负责…，Y 负责…"，用户可打断修正（这是沟通轮的一部分）。

### 2.5 反馈形态示例（用户视角的完整对话流）

```
用户：/team 我想把 A 模块实现出来
orchestrator：好的。开工前确认两件事：1) A 模块的完成定义是？（能跑通什么场景算完）
              2) 需要我安排一个只读评审角色盯着代码，还是先只做实现？
用户：先实现，跑通 3 个用例算完；评审后面再说。
orchestrator：明白。我建 1 个角色：implementer——负责实现 A 模块并跑通 3 个用例，
              产出代码 + 验证记录。开建…
              ✓ 已创建会话 session-xxx 并注入角色纪律，任务已派发。
              [后续] 角色回复、进度、报告都会汇总到这里。
```

## 3. 角色自我认知协议（底层关键）

**被 create 出来的每个角色会话，都必须知道自己正运行在 orchestra 上**。注入内容（协议清单）：

| # | 认知条目 | 内容 |
|---|---|---|
| 1 | 身份 | 你运行在 orchestra 编排中，是角色「<角色名>」 |
| 2 | **回信地址** | 你的 orchestrator（调度者）是会话 `session-<executor>`：**回复走 a2a_reply 给它，不直接回复用户**；用户只通过 orchestrator 与你交互 |
| 3 | 任务来源 | 任务由 orchestrator 通过 a2a_send 派发；每条任务自包含（不依赖你的历史对话） |
| 4 | 交接方式 | 产出/报告用 orchestra_report 写入 `orchestra/reports/`，回复 orchestrator 时返回路径。**每次写盘自动记账**：调用角色的 rounds +1、lastReport = 新路径（orchestra_team 反映实际交付） |
| 5 | 唤醒语义 | 冷会话会被自动 resume（a2a 层行为）；回复后可等待下一任务 |

**注入时机与载体**（沿用现有分工：preset 承载完整纪律，welcome 是轻量指针）：

- 静态模板：preset（如 orchestra-reviewer）+ welcome（含 executor 语义，现有实现已带"executor 是发起本消息的会话"）。
- **动态编排：executor sessionId 是运行时事实，不能写死在 preset 里**——由 orchestrator 在创建时注入（welcome 消息中显式带上回信地址 `session-<executor>`）。

**协议化要求**：所有角色（模板或动态）收到的注入内容必须一致地包含第 2 条（回信地址）；这是"角色不直接对用户说话"的纪律底座。

## 4. 场景细化 A：看模板（可选预设入口）

### 4.1 `orchestra_topologies`（缺口，P1）

- 输入：无。
- 返回：`{ templates: [{ id, name, description, source: "bundled"|"user", roles: [{ id, name, preset?, sandbox?, maxRounds? }] }] }`（实现只暴露模板可复用字段；model/mode 是拓扑内的角色声明配置，不在清单工具上透出）
- 反馈：模板名 + 一句话说明 + 角色概要；**末尾引导两条路径**："用某个内置模板去执行一个任务，还是想直接描述你的目标，我来给你编排角色？"

### 4.2 定位说明

- 模板在 v2 定位中是**可选预设**：有现成模板（如 duo）省事；没有匹配模板时，动态编排（§2）是完整替代——**模板不再是开局的前置条件**。

## 5. 场景细化 B：结案 / 开局闭环

### 5.1 `orchestra_dismiss`（缺口，P1）

- 输入：无（作用于当前目录实例）。
- 动作：实例结案——`state/team.json` 归档到 `<cwd>/orchestra/archive/team-<topology>-<createdAt>.json`，目录恢复可开局。
- **dismiss 只归档实例记录，不停角色会话**：角色会话是独立资产（其日志/报告/进度保留，可继续被 a2a 访问）；用户可在 GUI 自行关闭旧会话。
- 返回：归档路径 + 后续选项（"开新局 / 查看归档"）。

### 5.2 `orchestra_create` 冲突引导（增强，P1）

- 同目录已有**未结案**实例 → 拒绝，并引导："当前目录有未结案的团队，先结案（orchestra_dismiss）或换个目录"。
- 已结案（归档）实例不挡路：归档即释放目录。

### 5.3 与 `/team` 的关系

- `/team` 动态编排与 `orchestra_create`（模板开局）共用实例层（team.json）：**一目录一实例**；动态编排也走同样的实例记录/结案生命周期，只是角色集来自对话而非模板。

## 6. 数据模型分层

### 6.1 模板层（可选预设，用户资产）

- 位置：`<cwd>/orchestra/topologies/*.json`，用户可编辑、可新增。
- 来源：插件附赠（bundled）/ 用户自建（user）；兜底 = 插件内嵌 EMBEDDED_DUO。
- 变更语义：模板变更不影响已开局实例（实例是快照）。

### 6.2 实例层（运行快照）

- 位置：`<cwd>/orchestra/state/team.json`。
- 内容：topology 引用 + 角色映射（sessionId/preset/sandbox/轮次/报告路径）+ executor 会话。
- 生命周期：创建 → 运行（activate 可恢复）→ 结案（归档）→ 目录可重新开局。
- 约束：一目录一实例（当前语义）；create 遇未结案实例 → 拒绝并引导结案。

### 6.3 归档（结案产物）

- 位置：`<cwd>/orchestra/archive/team-<topology>-<createdAt>.json`（命名草案）。
- 归档是保留历史，不是删除；模板、报告、角色会话日志各自保留。

## 7. 现状 vs 缺口（按优先级）

> 2026-08-16 实现状态：P0/P1 已落地（v0.1.0 本地包，已过 headless E2E）；A/C/B 已实现并通过综合 E2E + 定向验证。
>
> **重要架构事实（2026-08-16 实测发现）**：DSH 中模型看到的工具结果是 `output.render` 的渲染文本（非原始 JSON）——工具的关键数据必须渲染进 render，模型才能使用。`orchestra_team`/`orchestra_topologies` 的 render 已按此原则设计。

| 优先级 | 能力 | 现状 | 缺口 |
|---|---|---|---|
| **P0** | 角色自我认知协议 | ✓ `sendRoleWelcome` 统一注入协议段（orchestrator 身份 / a2a_reply 回信 / 自包含任务 / report 交接） | 已实测 |
| **P0** | `/team` 动态编排 | ✓ `/team` 命令（handler 注入对话流）+ `orchestra_spawn`（动态建角色、实例记录 custom） | 已实测 |
| P1 | 结案/开局闭环 | ✓ `orchestra_dismiss`（归档 + 结案标记）+ create 冲突引导 | 已实测 |
| P1 | 看模板 | ✓ `orchestra_topologies`（用户模板 + bundled 兜底） | 已实测 |
| **A** | 大模板 + 分发 | ✓ trio 模板（implementer + reviewer）+ 内嵌模板表 `EMBEDDED_TEMPLATES`（用户目录同名文件自动覆盖，bundled 兜底） | 已实测（trio E2E 7/7） |
| **C** | 进度观察 | ✓ `orchestra_team` 角色增加 lastActivityAt / lastActivity（最近真实对话摘要，过滤注入消息） | 已实测 |
| **B** | spawn 增强 | ✓ `orchestra_spawn` 支持 templateId+roleId（从模板补角色）+ provider/model/**reasoningEffort** 覆盖（`installModelSelection` 注入路径已打通） | 已实测 |
| — | 线程可读性 | ✓ `a2a_list` 线程标注 `archived`（属于已结案实例），实例记录（archive/team-*.json）即"团队记忆" | 已实测 |
| — | 重启恢复 | ✓ `orchestra_activate` 真实重启场景实测（进程 A 建队 → 新进程 B 激活 → 派活闭环） | 已实测 |
| — | 核心角色命名 | ✓ **driver**（驾驶员）：协议/工具描述/systemPrompt 全部迁移 | 已落地 |
| — | 确定性路由 | ✓ systemPrompt：固定 flow 直连 + 只上报决策点 + 默认回退（消灭无意义路由） | 已落地 |
| — | 第三个模板 | ✓ **oracle**（深度推演搭档，read-only，取代 plan mode 的推演闭环） | 已落地 |
| — | GUI 设置页 | ✓ settings.section "orchestra" tab（client 面板 + host state 路由 `/plugins/orchestra-dsh/state`，轮询渲染模板/团队/归档） | 路由与 client bundle 已实测；面板渲染待 GUI 验收 |
| P2 | 模板文件分发（tgz 内置 topologies/） | ✗ | 当前用代码内嵌分发，文件分发后续可选 |
| — | 传输层 / 恢复 / 报告 | ✓ | 保持契约（a2a_*、orchestra_activate/report） |

## 8. 开放问题

1. ~~斜杠命令实现机制~~ **已解决（2026-08-16）**：`/team` 经 `commands.register`（peer 加 `@deepseek-ai/dsh-commands`），handler 把命令体作为用户消息 `followup` 注入接收 agent（`recordInput: false`，注入消息是权威记录），编排由模型走自然语言流程，不在命令代码里硬编码。
2. **动态编排的实例记录**：`/team` 创建的动态团队（无模板 id）在 team.json 里 topology 字段记什么？**已定：`topology: "custom"` + 角色集快照（2026-08-17 冒烟确认）**。
3. **开局沟通的边界**：2 轮沟通后用户仍未给全信息——用默认方案开局并明示假设，还是中止？草案：带默认方案开局。
4. 模板分发机制（bundled 放哪、user 覆盖规则）——P2。
5. 大模板形态（"一大"）——P2。
6. 归档命名与保留策略；多实例并行是否需要——远期。
